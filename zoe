const f=function(PERSONS,age){
    return new Promise((resolve,reject)=>{
        let target=age
        const reslt=PERSONS.filter(PERSONS=>PERSONS.target>=45)
        resolve('list aftre filttering')
        reject('error massage')
        cconsole.log(target)
        cconsole.log(f)
      
    })
   
 }



